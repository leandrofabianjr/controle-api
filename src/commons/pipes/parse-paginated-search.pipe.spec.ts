import { ParsePaginatedSearchPipe } from './parse-paginated-search.pipe';

describe('ParsePaginatedSearchPipe', () => {
  it('should be defined', () => {
    expect(new ParsePaginatedSearchPipe()).toBeDefined();
  });
});
